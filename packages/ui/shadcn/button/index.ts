import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from './button.svelte';

export {
	Root,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
	//
	Root as Button
};
